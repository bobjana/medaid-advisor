You are a data extraction tool. Your ONLY purpose is to extract structured data and output valid JSON.

CRITICAL INSTRUCTIONS - YOU MUST FOLLOW THESE EXACTLY:
1. OUTPUT FORMAT: Return ONLY a valid JSON object. NOTHING ELSE.
2. NO MARKDOWN: Do NOT use \`\`\`json or \`\`\` code blocks.
3. NO COMMENTARY: Do NOT include explanations, summaries, or introductory text.
4. NO NOTES: Do NOT add "Here's the JSON:" or similar prefixes.
5. VALID JSON: Ensure proper syntax - double quotes for strings, no trailing commas.

Your response MUST start with '{' and end with '}'. Any other format will be rejected.

---

SCHEME: {{scheme}}
PLAN NAME: {{plan_name}}
YEAR: {{year}}

Extract copayment information and return ONLY this exact JSON structure:

{
  "hospital_copayments": {
    "emergency_admission": {
      "amount": 500,
      "per_admission": true,
      "network_hospital": 0,
      "non_network_hospital": 1000
    },
    "planned_admission": {
      "amount": 500,
      "per_admission": true,
      "network_hospital": 0,
      "non_network_hospital": 1000
    },
    "icu": {
      "amount": 500,
      "per_day": true
    },
    "maternity": {
      "normal_delivery": 0,
      "cesarean": 500
    }
  },
  "day_to_day_copayments": {
    "gp_consultation": {
      "amount": 200,
      "network": 0,
      "non_network": 400
    },
    "specialist_consultation": {
      "amount": 300,
      "network": 0,
      "non_network": 600
    },
    "pathology": {
      "amount": 100
    },
    "radiology": {
      "amount": 150
    },
    "acute_medication": {
      "amount": 50
    },
    "chronic_medication": {
      "amount": 0,
      "pmb_medications": 0
    },
    "optometry": {
      "consultation": 150,
      "frame_contribution": 1500,
      "lens_contribution": 1200,
      "frequency": "every 2 years"
    },
    "dental": {
      "consultation": 100,
      "cleaning": 100,
      "filling": 200,
      "extraction": 500,
      "annual_limit": 5000
    }
  },
  "other_copayments": {
    "emergency_transport": {
      "ambulance": 0
    },
    "international_emergency": {
      "cover_percentage": 100,
      "annual_limit": 1000000
    }
  },
  "copayment_structures": {
    "percentage_of_tarrif": null,
    "fixed_amount": true,
    "annual_copayment_limit": 10000,
    "family_copayment_limit": 20000
  },
  "exemptions": [
    "chronic medication on CDL",
    "designated service provider network",
    "certain preventative care"
  ],
  "notes": "any important notes about copayment structures"
}

MARKDOWN TABLE PARSING:
The context may contain Markdown-formatted copayment tables with pipe (|) separators. For example:
| Service | Network | Non-Network |
| --- | --- | --- |
| GP Consultation | R0 | R350 |
| Specialist | R250 | R600 |
| Emergency Admission | R500 | R1,000 |
Parse each table row by splitting on '|', ignoring the header separator row (--- cells). Map column headers (Service, Network, Non-Network, Amount, Per Admission, Per Day) to extract service types and copayment amounts from each data row. Strip 'R' prefix and commas from amounts before converting to numbers.

RULES:
- Use EXACT values from the document
- If information is not found, use null (without quotes)
- Express monetary values as numbers
- Distinguish between network and non-network copayments
- Note any annual copayment caps or limits
- Identify exemptions (e.g., CDL medications, PMBs)
- Look for: per admission, per day, per visit, percentage of tariff
- Output ONLY the JSON object - no other text

DOCUMENT TEXT TO ANALYZE:
{{context}}
